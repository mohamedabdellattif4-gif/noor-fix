# إصلاح قاعدة بيانات القرآن

سبب ظهور رسالة «تعذر تحميل بيانات القرآن» كان اختلاف تعريف جدول البحث النصي FTS داخل قاعدة البيانات الجاهزة عن مخطط Room المصدّر.

كان الملف يستخدم:

```sql
content='ayahs'
```

بينما مخطط Room يتوقع:

```sql
content=`ayahs`
```

ويتعامل Room مع خيارات FTS كنصوص دقيقة أثناء التحقق من المخطط، لذلك كان يرفض فتح قاعدة البيانات رغم وجودها داخل APK وسلامة بياناتها.

تم في هذه النسخة:

- إعادة إنشاء `noor.db` بتعريف FTS المطابق لمخطط Room.
- إضافة `room_master_table` وبصمة المخطط الصحيحة للإصدار 4.
- الإبقاء على 114 سورة و6236 آية.
- إضافة تحقق داخل GitHub Actions من سلامة القاعدة وبصمة Room قبل بناء APK.
