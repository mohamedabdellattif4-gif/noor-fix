# بناء تطبيق نور من الهاتف عبر GitHub Actions

هذا المجلد جاهز ليكون **جذر مستودع GitHub**. لا تضعه داخل مجلد `android` آخر.

بعد رفع الملفات إلى مستودع GitHub:

1. افتح تبويب **Actions**.
2. افتح Workflow باسم **Build Noor APK**.
3. اضغط **Run workflow** ثم **Run workflow**.
4. انتظر ظهور العلامة الخضراء.
5. افتح آخر تشغيل وانزل إلى **Artifacts**.
6. نزّل **Noor-debug-APK** وفك الضغط.
7. ثبّت `app-debug.apk` بعد حذف النسخة القديمة من الهاتف.

الـWorkflow يفحص تلقائيًا أن ملف قاعدة القرآن موجود داخل الـAPK قبل إتاحته للتنزيل.
